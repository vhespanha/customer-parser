[package]
name = "customer-parser"
version = "0.1.3"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
csv = "1.1"
tokio = { version = "1", features = ["full"] }
mpi = { version = "0.6", features = ["full"] }
